import tkinter as tk
from tkinter import messagebox, simpledialog, filedialog
import json
from tkinter import ttk

# Initialize an empty recipe dictionary to store data in memory
recipes = {}

# Function to update the recipe list in the GUI


def update_recipe_list():
    recipe_list.delete(0, tk.END)
    for recipe_name in recipes:
        recipe_list.insert(tk.END, recipe_name)

# Create the main window
root = tk.Tk()
root.title("Recipe Management System")
root.geometry("600x700")
root.tk.call("source", "azure.tcl")
root.tk.call("set_theme", "light")

#create GUI elemnts
add_button = tk.Button(root, text="Add Recipe", command=add_recipe)
add_button.grid(row=5, column=0, padx=5, pady=10)

edit_button = tk.Button(root, text="Edit Recipe", command=edit_recipe)
edit_button.grid(row=5, column=1, padx=5, pady=10)

delete_button = tk.Button(root, text="Delete Recipe", command=delete_recipe)
delete_button.grid(row=5, column=2, padx=5, pady=10)

export_button = tk.Button(root, text="Export Recipes", command=export_recipes)
export_button.grid(row=6, column=0, padx=5, pady=10)

import_button = tk.Button(root, text="Import Recipes", command=import_recipes)
import_button.grid(row=6, column=1, padx=5, pady=10)

view_button = tk.Button(root, text="View Recipes", command=view_recipes)
view_button.grid(row=6, column=2, padx=5, pady=10)

#reads json
def fileReader():
    with open("test.json", "r") as file:
        recipes = json.load(file)
        if len(recipes) != 0:
            return recipes
            
# Load initial data if available (e.g., from a JSON file)
try:
    with open("test.json", "r") as file:
        recipes = json.load(file)
        if len(recipes) != 0:
            update_recipe_list()
except FileNotFoundError:
    pass

# Apply the theme to the entire program
root.tk.call("ttk::style", "theme", "use", "azure-light")
root.mainloop()

